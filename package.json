{
  "name": "crud",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "canela": "json-server server/db.json --port 3001 --watch"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  }
}
